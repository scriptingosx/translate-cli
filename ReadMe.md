# macOS translate CLI

macOS command line tool to translate input using the built-in macOS Translation Service

## What it does

This command line tool translates text passed as arguments from on language to another.

